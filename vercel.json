{
    "version": 2,
    "build": {
      "env": {
        "NODE_ENV": "production"
      },
      "commands": [
        "npm install --force"
      ]
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "server.js"
      }
    ]
  }